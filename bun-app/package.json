{
  "name": "bun-search-app",
  "version": "1.0.0",
  "description": "Search server using Bun.serve() with HTTP/3 and multithreading",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts",
    "cluster": "bun run src/cluster.ts",
    "test": "bun test",
    "generate-certs": "mkdir -p certs && openssl req -x509 -newkey rsa:4096 -keyout certs/key.pem -out certs/cert.pem -days 365 -nodes -subj '/CN=localhost'"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  }
}
